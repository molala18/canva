import React from "react";

const SideBar = () => {
  return (
    <aside className="w-[100px] flex flex-col h-full bg-white border-r overflow-y-auto">
      sidebar
    </aside>
  );
};

export default SideBar;
